import { StyleSheet } from 'react-native';
import { PALETTE } from '../../styles/colors';

export const styles = StyleSheet.create({
  handleIndicatorStyle: {
    backgroundColor: PALETTE.secondary.darkWine60,
    height: 4,
  },
});
