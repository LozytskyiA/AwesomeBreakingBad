import { BlurView } from '@react-native-community/blur';
import styled from 'styled-components/native';

export const BlurStyles = {
  BlurWrapper: styled(BlurView)`
    position: absolute;
    top: 0px;
    left: 0px;
    bottom: 0px;
    right: 0px;
  `,
};
